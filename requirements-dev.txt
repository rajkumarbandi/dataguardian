# DataGuardian — Development & Testing Dependencies
#
# Install the full development stack:
#   pip install -e ".[dev,spark]"
#
# Or install from this file directly:
#   pip install -r requirements-dev.txt
#
# Production runtime dependencies are declared in pyproject.toml [project.dependencies].
# This file covers tooling that is never shipped to Databricks.

# ------------------------------------------------------------------
# Runtime (same as requirements.txt — needed for local execution)
# ------------------------------------------------------------------
pyyaml>=6.0
pydantic>=2.5
structlog>=24.2

# ------------------------------------------------------------------
# Test framework
# ------------------------------------------------------------------
pytest>=8.1
pytest-cov>=5.0
pytest-mock>=3.14
chispa>=0.9           # PySpark DataFrame assertion library

# ------------------------------------------------------------------
# Spark — for local integration tests (not needed on Databricks)
# ------------------------------------------------------------------
pyspark>=3.5
delta-spark>=3.1

# ------------------------------------------------------------------
# Linting & formatting
# ------------------------------------------------------------------
ruff>=0.4

# ------------------------------------------------------------------
# Type checking
# ------------------------------------------------------------------
mypy>=1.10
types-pyyaml>=6.0

# ------------------------------------------------------------------
# Security scanning
# ------------------------------------------------------------------
bandit>=1.7

# ------------------------------------------------------------------
# Pre-commit hooks
# ------------------------------------------------------------------
pre-commit>=3.7
